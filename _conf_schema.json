{
    "is_auto_parse": {
        "description": "是否自动解析B站链接",
        "type": "bool",
        "hint": "",
        "default": true
    },
    "is_auto_pack": {
        "description": "是否将解析结果打包为消息集合",
        "type": "bool",
        "hint": "需要在任何wechat平台使用请禁用此项",
        "default": true
    },
    "service_message": {
        "description": "解析成功后发送的提示文字",
        "type": "text",
        "hint": "留空则不发送提示文字",
        "default": "B站bot为您服务 ٩( 'ω' )و"
    },
    "desc_template": {
        "description": "标题/作者/简介 文本模板",
        "type": "text",
        "hint": "可用变量: {title} {author} {desc}",
        "default": "标题：{title}\n作者：{author}\n简介：{desc}"
    },
    "max_video_size_mb": {
        "description": "最大允许发送的视频大小(MB)",
        "type": "float",
        "hint": "超过此大小的视频将被跳过，设置为0表示不限制",
        "default": 0.0
    },
    "oversize_message": {
        "description": "视频大小超出限制时的提示",
        "type": "text",
        "hint": "可用变量: {current_size} {max_size}，留空则不发送",
        "default": "当前视频大小为 {current_size} MB，超过限制的 {max_size} MB，已为您跳过发送。"
    }
}
